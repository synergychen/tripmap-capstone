<tr class="location-stop-row td-row" data-trip-id="<%= stop.trip_id%>"
  data-stop-id="<%= stop.id %>">
  <td class="stop-order"><%= stop.order %></td>
  <td class="location-name"><%= stop.location.name %></td>
  <td class="location-address"><%= stop.location.address %></td>
  <td class="stop-stay_time td-time" val="<%= stop.stay_time %>"></td>
  <td class="to-next-stop td-time"></td>

  <% if current_user.owner?(@trip) %>
    <td>
      <%= button_to "Delete", [stop.trip, stop], method: :delete, class:
        "delete-stop-button" %>
    </td>
  <% end %>
</tr>
