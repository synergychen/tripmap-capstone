<h3>View Trip to <%= @trip.city %> on <%= @trip.starts_on %></h3>

<ul>
  <li>Starts On: <%= @trip.starts_on %></li>
  <li>City: <%= @trip.city %></li>
  <li>Completed: <%= @trip.completed %></li>
</ul>

<%= link_to "Edit", edit_trip_path(@trip) %>

<%= button_to "Delete", @trip, method: :delete %>
